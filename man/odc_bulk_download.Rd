% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odc_bulk_download.R
\name{odc_bulk_download}
\alias{odc_bulk_download}
\title{Download and extract EPC data files from ZIP archives}
\usage{
odc_bulk_download(
  file_name,
  destination_path,
  type = c("certificate", "recommendation"),
  keep_zip = FALSE
)
}
\arguments{
\item{file_name}{\code{character} File name to be downloaded (extension included)
Should be a valid file name from \code{odc_get_file()} or \code{odc_get_file_list()}.}

\item{destination_path}{\code{character} Directory path where extracted
CSV file will be saved. Directory will be created if it doesn't exist.}

\item{type}{\code{character} String specifying which data type to extract from
the ZIP archive. Must be either "certificate" (default) or "recommendation".}

\item{keep_zip}{\code{logical} Should the downloaded ZIP file be kept? If yes, it will
be saved in \code{destination_path}. Default is \code{FALSE} (only extracted CSV is kept).}
}
\value{
Invisibly returns \code{NULL}. The function's primary purpose is the
side effect of saving extracted CSV data to disk.
}
\description{
Downloads specified ZIP files from the EPC Open Data API, extracts either
certificate or recommendation CSV data, and saves it to the destination path.
Handles authentication, error checking, and cleanup of temporary files.
}
\examples{
\dontrun{
# Download and extract domestic certificate data for London
odc_bulk_download(
  file_name = "domestic-E08000025.zip",
  destination_path = here::here("downloads"),
  type = "certificate"
)

# Download and keep the ZIP file along with extracted recommendations
odc_bulk_download(
  file_name = "non_domestic-E08000025.csv.zip",
  destination_path = here::here("downloads"),
  type = "recommendation",
  keep_zip = TRUE
)
}
}
