% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odc_api_key.R
\name{odc_set_key}
\alias{odc_set_key}
\title{Set your Open Data Communities API credentials}
\usage{
odc_set_key(odc_user = NULL, odc_key = NULL, overwrite = FALSE)
}
\arguments{
\item{odc_user}{\code{character} Your Open Data Communities username (usually your
email address). If \code{NULL}, the function will abort.}

\item{odc_key}{\code{character} Your Open Data Communities API key. If \code{NULL},
the function will abort.}

\item{overwrite}{\code{logical} If \code{TRUE}, will overwrite an existing \code{ODC_API_KEY}
environment variable. Defaults to \code{FALSE}, preventing accidental overwrites.}
}
\value{
Returns \code{NULL}. Called primarily for its side effect of
setting the environment variable.
}
\description{
\code{odc_set_key()} stores your Open Data Communities username and API key
as an environment variable (\code{ODC_API_KEY}) for use in all subsequent API calls.
The credentials are combined and encoded in Base64 as required for the API's
HTTP Basic Authentication.
}
\details{
This function only needs to be run once per R session, or whenever your
credentials change. The key is stored in the \code{ODC_API_KEY} environment
variable for the current session.

For security, it is recommended \emph{not} to hard-code credentials in your
scripts. Consider using this function interactively or sourcing it from a
secure location.
}
\examples{
\dontrun{
# Set your credentials for the first time
odc_set_key(
  odc_user = "email@example.com",
  odc_key = "your_api_key_here"
)

# Overwrite existing credentials (if needed)
odc_set_key(
  odc_user = "email@example.com",
  odc_key = "new_api_key",
  overwrite = TRUE
)
}
}
