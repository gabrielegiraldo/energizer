# Fetch available EPC code tables

Fetch available EPC code tables

## Usage

``` r
epb_get_codes()
```

## Value

A tibble with one `code` column.

## Examples

``` r
if (FALSE) { # \dontrun{
epb_get_codes()
} # }
```
