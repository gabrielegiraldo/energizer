# Fetch available EPC code tables

Fetch available EPC code tables

## Usage

``` r
get_codes()
```

## Value

A tibble with one `code` column.

## Examples

``` r
if (FALSE) { # \dontrun{
get_codes()
} # }
```
